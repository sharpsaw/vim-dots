" You gotta be down with gqip, gqq, etc. But this is the right thing to say:
au FileType mail set tw=75

hi def link mailQuoted1		Type

" Oh snap. I wonder where that script is.
au FileType mail map OR :w<CR>:!~/bin/mail-lint %<CR>:e<CR>
